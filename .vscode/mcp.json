{
  "servers": {
    "ai-dev-cockpit": {
      "type": "stdio",
      "command": "node",
      "args": [
        "--env-file-if-exists=.env",
        "--import",
        "tsx",
        "${workspaceFolder}/apps/mcp-server/index.ts"
      ],
      "cwd": "${workspaceFolder}"
    }
  }
}
