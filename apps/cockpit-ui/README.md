# Cockpit UI

React e MCP Apps SDK. Recebe o contrato `Analysis` do host, renderiza as quatro áreas do cockpit e chama tools pelo bridge. Vite gera um HTML único em `dist/ui`. A UI exige host compatível; abrir o HTML diretamente não valida a integração.
